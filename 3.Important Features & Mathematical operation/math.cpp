#include <iostream>
#include<cmath>
using namespace std;
int main()
{
    double x=3,y=4,z; 
    z = max(x,y);
    cout<<z<<'\n';
    z = min(x,y);
    cout<<z<<'\n';
    z = min(x,y);
    cout<<z<<'\n';
    z = sqrt(9);
    cout<<z<<'\n';




}